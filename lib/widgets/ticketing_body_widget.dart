import 'package:flutter/material.dart';

class TicketingBodyWidget extends StatefulWidget {
  const TicketingBodyWidget({Key? key}) : super(key: key);

  @override
  State<TicketingBodyWidget> createState() => _TicketingBodyWidgetState();
}

class _TicketingBodyWidgetState extends State<TicketingBodyWidget> {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
